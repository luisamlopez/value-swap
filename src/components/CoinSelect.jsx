import { Box } from "@mui/material";

const CoinSelect = () => {
    return (
        <Box sx={{
            color: '#fff', zIndex: 1
        }}>
            hola
        </Box>)
}

export default CoinSelect;